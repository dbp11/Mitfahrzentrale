require 'spec_helper'

describe Passenger do
  before(:each) do
    @attr = {
      :user_id => 564,
      :trip_id => 12,
      :confirmed => true
    }
  end
  
  #Test ob Validations funktionieren
end
