%h1 Listing messages

- @messages.each do |message|
	%table.message_table
		%tr.message_table_row
			%td.message_table_left
				= "Nachricht an"
				= link_to(message.receiver.name, url_for(message.receiver))
			%td.message_table_right
				= message.created_at.strftime("%d.%m.%y %H:%M")
		%tr.message_table_row
			%td.message_table_both{:colspan => "2"}
				= message.message
		%tr.message_table_row
			%td.message_table_both{:colspan => "2"}
				=link_to("Neu Nachricht", url_for(:action => "new", :controller => "messages", :id => message.receiver.id))
				= "|"
				= link_to("Löschen", url_for(:action => "update", :controller => "messages", :id => message.id, :who => "writer"), :method => :put)
