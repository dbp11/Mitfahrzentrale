%h1 Listing messages

- @messages.each do |message|
	%table.message_table
		%tr.message_table_row
			%td.message_table_left
				= "Nachricht von"
				= link_to(message.writer.name, url_for(message.writer))
			%td.message_table_right
				= message.created_at.strftime("%d.%m.%y %H:%M")
		%tr.message_table_row
			%td.message_table_both{:colspan => "2"}
				= message.message
		%tr.message_table_row
			%td.message_table_both{:colspan => "2"}
				=link_to("Antworten", url_for(:action => "new", :controller => "messages", :id => message.writer.id))
				= "|"
				= link_to("Löschen", url_for(:action => "update", :controller => "messages", :id => message.id, :who => "receiver"), :method => :put)
