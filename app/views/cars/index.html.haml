= stylesheet_link_tag :cars
.cars
  %h1 Deine Fahrzeuge

  
  
  %table
    %tr
      %th Bild
      %th Modell
      %th Anzahl der Sitzpl&auml;tze
      %th Verbrauch
      %th Raucher
      %th Kennzeichen
      %th Beschreibung
      %th Anzeigen  
      %th Editieren
      %th Löschen

    - @cars.each do |car|
      %tr
        - if car.carpic?
          %td= image_tag car.carpic.url(:thumb)
        - else
          %td= "Kein Bild"
        %td= car.car_type
        %td= car.seats
        %td= car.fuel_consumption
        %td
          = car.smoker? ? "Ja" : "Nein"
        %td= car.licence
        %td= car.description
        %td= link_to 'Details', car
        %td= link_to 'Bearbeiten', edit_car_path(car)
        %td= link_to 'Löschen', car, :confirm => 'Bist du sicher?', :method => :delete

  %a{:href => "/cars/new"}
    Auto hinzuf&uuml;gen

